// import { useContext } from "react";
import React from "react";
// import { FontContext } from "../components/Font";

export default function Settings() {
  // const { fontFamily, setFontFamily } = useContext();

  // const handleChange = (e) => setFontFamily(e.target.value);
  return (
    <div className="p-4">
      <h1 className="text-2x1 font-bold mb-4">Settings</h1>
      {/* 
      <label htmlFor="fontSelect">Choose font:</label>
      <select id="fontSelect" value={fontFamily} onChange={handleChange}>
        <option value="Arial">Arial</option>
        <option value="Courier New">Courier New</option>
        <option value="Times New Roman">Times New Roman</option>
        <option value="Comic Sans">Comic Sans</option>
      </select> */}
    </div>
  );
}
